#pragma once
#include "Defines.h"

//This function display the fibonacci sequence, up to maxValue, given as a parameter.
//This implementation works with a simply history buffer.
void fibonacci(int maxValue);