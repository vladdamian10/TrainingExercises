#pragma once
#include "Defines.h"

//This function will return the number which is occuring most in a vector.
//ATTN!: The input vector must have only positive integer numbers.
int getNumberWithMaxOccurence( std::vector<int>& inputArr);